class Program {
   public void run() {
      boolean b;
      b = false;
      do {
      } while (false);
      do {
      } while (b);
      int n, i, j;
      i = 0;
      j = 0;
      n = 10;
      do {
         do {
            ++i;
            write (i);
         } while (i < n);
         ++j;
      } while (j < n);
   }
}

